# === BASE DE DONNÉES ===
DATABASE_URL="postgresql://user:password@localhost:5432/flexwork?schema=public"

# === AUTHENTIFICATION (NextAuth.js) ===
NEXT_PUBLIC_APP_URL="http://localhost:3000"
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-random-secret-here"

# === WEBSOCKET (Socket.io) ===
NEXT_PUBLIC_WS_URL="http://localhost:3001"
WS_PORT="3001"

# === STRIPE ===
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_..."
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."

# === TRUSTENGINE ===
TRUSTENGINE_API_KEY="te_..."
TRUSTENGINE_WEBHOOK_SECRET="te_whsec_..."

# === REDIS (BullMQ — Queues et Jobs) ===
REDIS_URL="redis://localhost:6379"

# === MAILPIT / SMTP (Emails) ===
SMTP_HOST="localhost"
SMTP_PORT="1025"
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM="Flexwork <noreply@flexwork.app>"
SMTP_SECURE="false"

# === OPENAI (Pour IA Uma Recruiter) ===
OPENAI_API_KEY="sk-..."
